Generate simple web app SPA that is PWA and can be shown offline with remebered variables like device_IP. The app should show one field where device_IP can be edited, time to be set in another variable, additionally 2 more fields with wifi_name and wifi_password. At the end of the formular should be placed a button Save that causes sending those data to device with IP stored in the device_IP variable and endpoint /store. All fields with data should be sent via this endpoint in json format.

I'll help you create a Progressive Web App (PWA) that can work offline and manage device configuration data.



```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#4a90e2">
    <title>Device Config</title>
    <link rel="manifest" href="manifest.json">
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 600px;
            margin: 20px auto;
            padding: 0 20px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }
        input {
            width: 100%;
            padding: 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        button {
            background-color: #4a90e2;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            width: 100%;
        }
        button:hover {
            background-color: #357abd;
        }
        .status {
            margin-top: 15px;
            padding: 10px;
            border-radius: 4px;
            display: none;
        }
        .success {
            background-color: #d4edda;
            color: #155724;
        }
        .error {
            background-color: #f8d7da;
            color: #721c24;
        }
    </style>
</head>
<body>
    <h1>Device Configuration</h1>
    <form id="configForm">
        <div class="form-group">
            <label for="deviceIP">Device IP:</label>
            <input type="text" id="deviceIP" required pattern="^(?:[0-9]{1,3}\.){3}[0-9]{1,3}$">
        </div>
        <div class="form-group">
            <label for="time">Time:</label>
            <input type="datetime-local" id="time" required>
        </div>
        <div class="form-group">
            <label for="wifiName">WiFi Name:</label>
            <input type="text" id="wifiName" required>
        </div>
        <div class="form-group">
            <label for="wifiPassword">WiFi Password:</label>
            <input type="password" id="wifiPassword" required>
        </div>
        <button type="submit">Save</button>
    </form>
    <div id="status" class="status"></div>

    <script>
        // Register service worker
        if ('serviceWorker' in navigator) {
            navigator.serviceWorker.register('sw.js')
                .then(registration => console.log('ServiceWorker registered'))
                .catch(error => console.log('ServiceWorker registration failed:', error));
        }

        // Load saved data from localStorage
        document.addEventListener('DOMContentLoaded', () => {
            const savedData = JSON.parse(localStorage.getItem('deviceConfig') || '{}');
            if (savedData.deviceIP) document.getElementById('deviceIP').value = savedData.deviceIP;
            if (savedData.time) document.getElementById('time').value = savedData.time;
            if (savedData.wifiName) document.getElementById('wifiName').value = savedData.wifiName;
            if (savedData.wifiPassword) document.getElementById('wifiPassword').value = savedData.wifiPassword;
        });

        // Handle form submission
        document.getElementById('configForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const formData = {
                deviceIP: document.getElementById('deviceIP').value,
                time: document.getElementById('time').value,
                wifiName: document.getElementById('wifiName').value,
                wifiPassword: document.getElementById('wifiPassword').value
            };

            // Save to localStorage
            localStorage.setItem('deviceConfig', JSON.stringify(formData));

            const statusDiv = document.getElementById('status');
            
            try {
                const response = await fetch(`http://${formData.deviceIP}/store`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(formData)
                });

                if (response.ok) {
                    statusDiv.textContent = 'Configuration saved successfully!';
                    statusDiv.className = 'status success';
                } else {
                    throw new Error('Failed to save configuration');
                }
            } catch (error) {
                statusDiv.textContent = 'Error saving configuration. Data saved locally.';
                statusDiv.className = 'status error';
            }
            statusDiv.style.display = 'block';
        });
    </script>
</body>
</html>

```

```javascript
// sw.js
const CACHE_NAME = 'device-config-v1';
const ASSETS_TO_CACHE = [
    '/',
    '/index.html',
    '/manifest.json'
];

self.addEventListener('install', (event) => {
    event.waitUntil(
        caches.open(CACHE_NAME)
            .then((cache) => cache.addAll(ASSETS_TO_CACHE))
    );
});

self.addEventListener('fetch', (event) => {
    event.respondWith(
        caches.match(event.request)
            .then((response) => response || fetch(event.request))
    );
});

```

```json
{
    "name": "Device Configuration",
    "short_name": "DeviceConfig",
    "start_url": "/",
    "display": "standalone",
    "background_color": "#ffffff",
    "theme_color": "#4a90e2",
    "icons": [
        {
            "src": "icon.png",
            "sizes": "192x192",
            "type": "image/png"
        }
    ]
}

```

I've created a complete Progressive Web App that meets your requirements. Here's what each component does:

1. The main HTML file includes:
   - A responsive form with all required fields
   - Local storage integration to persist data
   - Offline functionality
   - Form validation
   - Status messages for success/failure
   - Clean, modern styling

2. The Service Worker (sw.js):
   - Enables offline functionality
   - Caches important assets
   - Handles network requests

3. The Web App Manifest (manifest.json):
   - Makes the app installable
   - Defines app appearance and behavior

Key features:
- The app saves data locally even when offline
- Validates IP address format
- Provides visual feedback for form submission
- Remembers previously entered values
- Works offline after first load
- Can be installed as a standalone app

To deploy this:
1. Save these files in your web server directory
2. Create an icon.png file (192x192 pixels) for the app icon
3. Serve over HTTPS (required for PWAs)
4. Access through a web browser and optionally install as a PWA

When the form is submitted, it will:
1. Save the data locally
2. Attempt to send to the device's IP address
3. Show success/error message
4. Work offline if needed

Would you like me to modify anything about the implementation or add any additional features?Wifi hotspot + wifi:
https://www.raspberrypi.org/forums/viewtopic.php?t=211542
